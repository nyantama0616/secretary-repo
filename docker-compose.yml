services:
  db:
    image: postgres:17
    ports:
      - '5432:5432'
    env_file:
      - ./apps/web/.env
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./docker/init:/docker-entrypoint-initdb.d:ro

volumes:
  db-data:
